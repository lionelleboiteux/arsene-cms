# arsene-cms — Red-gate traceability matrix

> Gate 3 (red) artifact. Tests only — no production code exists yet, by design.

**Status:** red gate | **Author:** Claude (Opus 5) | **Date:** 2026-08-11
**Suite:** 126 tests, 124 failing, 2 passing (see `03-red-evidence.v1.md`)

Every row below names the test(s) that cover an obligation. Tags (`AC-nn`,
`NFR-*`, `PROP-*`, `TELEMETRY-*`, `CONTRACT-*`, `DEC-*`, `ADR-*`, `E2E-*`) are
embedded in test names, so this matrix can be regenerated by grepping the
runner output.

---

## 0. Directory layout and module map

The suite is TypeScript + Vitest, run with `npm test` from the repo root — the
same toolchain, config shape and directory split as the sibling `pronos`
project, deliberately.

| Path | What it is |
|---|---|
| `tests/support/seams.ts` | The **single** place naming every production module the suite expects, with its expected signature. Every import is lazy, inside a function, so a missing module fails exactly one test. |
| `tests/support/fixtures.ts` | Deterministic fixtures; UUIDs match `contracts/openapi.yaml` examples. |
| `tests/support/imageFixtures.ts` | Real image bytes — a decodable JPEG, the same padded past 4 MB and past 20 MB, a corrupted JPEG, a PDF. Generated deterministically rather than committed, so no 20 MB blob enters git. |
| `tests/support/fakes.ts` | Fakes for the *collaborators* of a unit under test, so a handler test fails only because the handler is missing. |
| `tests/support/pg.ts` | Real Postgres 16 via Testcontainers + production-migration applier + seed helpers. |
| `tests/support/openapi.ts` | Loads Arsène's contract **and** the vendored pronos contract; builds Ajv 2020 validators from both. |
| `tests/support/prism.ts` | Prism mock lifecycle (consumer side, either document). |
| `tests/support/schemathesis.ts` | Schemathesis CLI runner (provider side). |
| `tests/unit/` | Pure-function domain tests, plus the real-codec image tests (fast, no DB, no browser). |
| `tests/telemetry/` | The 2 required events: shape + positive/negative emission. |
| `tests/db/` | Real Postgres: constraints, RLS, locking under real timing — and the seeded-DB public-site render pass. |
| `tests/contract/` | Both sides of Arsène's contract, plus the consumer side of pronos'. |
| `tests/e2e/` | The three journeys whose silent failure would be embarrassing. |

Production modules the tests expect (none exist yet — this is the red gate).
Paths follow the seams named in `02-architecture.v1.md` §1 "Testability
assessment".

| Expected production path | Seam |
|---|---|
| `src/domain/paste.ts` | `sanitizePastedHtml` — pure, raw HTML in / clean HTML out, no DOM |
| `src/domain/seo.ts` | `generateSlug`, `suggestMeta`, `buildStructuredData`, `buildSitemapEntry`, `generateAltText`, `runContentCheck` |
| `src/domain/lock.ts` | `isLockStale`, `evaluateLock`, `LOCK_STALENESS_MS`, `HEARTBEAT_INTERVAL_MS` — injectable `now` |
| `src/domain/autosave.ts` | `evaluateAutosave`, `restoreDraft`, `AUTOSAVE_INTERVAL_MS` — injectable `now` |
| `src/domain/taxonomy.ts` | `resolveCategoryPath` — league-then-type nesting |
| `src/domain/pronosEntry.ts` | `buildPronosEntry`, `CONFIDENCE_TIERS` |
| `src/images/optimize.ts` | `optimizeImage`, `MAX_UPLOAD_BYTES` — the real WebP/AVIF codec |
| `src/api/createDraft.ts` | `handleCreateDraft`, `handleOpenDraft` |
| `src/api/publishArticle.ts` | `handlePublishArticle` (`POST /v1/articles/{id}/publish`) |
| `src/api/uploadImage.ts` | `handleUploadImage` (`POST /v1/articles/{id}/images`) |
| `src/api/rateLimit.ts` | `createRateLimiter`, `PUBLISH_RATE_LIMIT_PER_MINUTE` |
| `src/api/client.ts` | `createArseneClient` (contract consumer) |
| `src/api/server.ts` | `startServer` (contract provider / e2e entrypoint) |
| `src/client/fixturePicker.ts` | `createFixturePicker` (consumer of pronos' API) |
| `src/site/render.ts` | `createSiteRenderer` → homepage / category / article / sitemap render pass |
| `src/telemetry/events.ts` | `REQUIRED_EVENT_TYPES`, `buildTelemetryEvent`, `createTelemetrySink` |
| `db/migrations/*.sql` | Expand-only production migrations (`02-architecture.v1.md` §6) |

---

## 1. Acceptance criteria — all 18 covered, none uncovered

| AC | Statement (abbreviated) | Test file | Test name(s) |
|---|---|---|---|
| AC-01 | Autosave fires after the interval and updates "Last saved at [time]" | `tests/unit/draft.test.ts` | `AC-01: after 35 seconds of typing with no manual save, the autosave tick saves the draft and stamps the indicator…`; `AC-01: a tick with nothing typed since the last save writes nothing…`; `AC-01: the shipped autosave interval is the assumed 30 seconds` |
| AC-02 | Reopening after a crash restores the last autosave | `tests/unit/draft.test.ts` | `AC-02: reopening a draft whose browser died restores the last autosaved version, not an earlier one` |
| AC-03 | Word/Docs paste keeps H2/H3, discards fonts and styling | `tests/unit/paste.test.ts`, `tests/unit/properties.test.ts` | `AC-03: pasting a Word document keeps its "Heading 2" paragraph as an H2…`; `AC-03a`–`AC-03e` (style attr, `<font>`, Google-Docs classes, H3 depth, script/`onclick`); `PROP-04`, `PROP-05` |
| AC-04 | Structured pronos entry stored as data, not free text | `tests/unit/pronosEntry.test.ts`, `tests/unit/publishArticle.test.ts`, `tests/db/schema.test.ts` | `AC-04: PSG 2-1 Marseille with tier "Indispensable" is stored as typed fields…`; `AC-04a/04b/04c` (bad tier, missing team, non-integer score); `AC-04: the shipped confidence tiers are exactly the three…`; `AC-04: publishing a Pronos article whose structured match fields are invalid is refused with 400 VALIDATION_FAILED` |
| AC-05 | Locking: second writer sees who holds it and cannot edit | `tests/unit/lock.test.ts`, `tests/db/schema.test.ts`, `tests/unit/publishArticle.test.ts`, `tests/contract/consumer.prism.test.ts` | `AC-05: writer B opening a draft writer A is actively editing is refused, and told who holds it`; `AC-05: writer A is never locked out of the draft they themselves hold`; `AC-05: a lock left behind by a crashed browser expires on its own…`; `AC-05: a draft nobody holds is editable`; `AC-05: while writer A's heartbeat is current, writer B's attempt to take the lock updates no row`; `AC-05: once writer A's lock is 91 seconds stale, writer B takes it…`; `AC-05: publishing a draft another writer currently holds… 409 DRAFT_LOCKED`; `CONTRACT-CONSUMER-publishArticle / AC-05` |
| AC-06 | Cover image is distinct from body images; only the cover is listed/shared | `tests/unit/uploadImage.test.ts`, `tests/db/publicSiteRender.test.ts` | `AC-06: uploading a new cover demotes the article's previous cover…`; `AC-06: a body-image upload never claims to have replaced a cover`; `AC-06: the category listing and the social preview both use the cover image, and never a body image` |
| AC-07 | 4 MB JPEG becomes a compressed WebP/AVIF; the original is not served | `tests/unit/imageOptimize.test.ts`, `tests/unit/uploadImage.test.ts` | `AC-07: a valid 4 MB JPEG is handled distinguishably` (real codec, real file); `AC-07: a 4 MB JPEG comes back as a compressed WebP/AVIF resource, and the bytes actually stored for visitors are far smaller than the upload` |
| AC-08 | Broken/unsupported image → clear error; never published broken | `tests/unit/imageOptimize.test.ts`, `tests/unit/uploadImage.test.ts`, `tests/unit/publishArticle.test.ts`, `tests/contract/consumer.prism.test.ts` | `AC-08a: a corrupted file that still sniffs as a JPEG…`; `AC-08b: a container this pipeline does not support at all…`; `AC-08: a file whose format cannot be recognised is refused… creates no image row at all`; `AC-08: a file that passes format detection but fails to decode becomes a failed image…`; `AC-08a/AC-08b: publishing … refused with 409 IMAGE_NOT_READY`; `CONTRACT-CONSUMER-uploadArticleImage / AC-08` |
| AC-09 | Site asset library is shared between writers | `tests/db/schema.test.ts` | `AC-09: a logo uploaded by one writer is listed for every other writer, with no re-upload` |
| AC-10 | Category creation open, nested league-then-type, no approval | `tests/unit/taxonomy.test.ts` | `AC-10: assigning an article to a league and type that do not exist creates both, nested league-then-type, with no approval step`; `AC-10: an existing league is reused rather than duplicated when only the type is new` |
| AC-11 | Empty category page says "No articles yet" | `tests/db/publicSiteRender.test.ts` | `AC-11: a category with no published article shows "No articles yet" rather than an error or a blank page` |
| AC-12 | Homepage: reverse chronological across leagues | `tests/db/publicSiteRender.test.ts` | `AC-12: the homepage lists every league's articles newest first, regardless of league` |
| AC-13 | Meta title/description pre-filled and editable at publish | `tests/unit/seo.test.ts`, `tests/unit/publishArticle.test.ts` | `AC-13: reaching the publish step yields a meta title and description pre-filled from the article…`; `AC-13: meta title and description edited by the writer at the publish step are used verbatim…` |
| AC-14 | Slug, JSON-LD and sitemap generated silently | `tests/unit/seo.test.ts`, `tests/unit/publishArticle.test.ts`, `tests/db/publicSiteRender.test.ts`, `tests/unit/properties.test.ts` | `AC-14: the URL slug is derived from the title with no writer action…`; `AC-14: the generated schema.org markup is a valid NewsArticle…`; `AC-14: the sitemap entry points at the article's canonical URL…`; `AC-14: a successful first publish returns the automatically generated slug, JSON-LD and sitemap entry…`; `AC-14: the published article page embeds its schema.org markup and the sitemap carries its canonical URL…`; `PROP-01`, `PROP-02` |
| AC-15 | Alt text auto-generated and overridable | `tests/unit/seo.test.ts`, `tests/unit/uploadImage.test.ts`, `tests/db/schema.test.ts` | `AC-15: alt text is generated from the article's own context rather than the file name`; `AC-15: a processed image carries auto-generated alt text, ready for the writer to override`; `AC-15: a writer can overwrite generated alt text with a direct row update, no endpoint involved` |
| AC-16 | Advisory SEO/AEO/GEO suggestions never block publishing | `tests/unit/seo.test.ts`, `tests/unit/publishArticle.test.ts` | `AC-16: an article with a too-short introduction is flagged, and no advisory ever claims to block publishing`; `AC-16: an article the content check flagged still publishes when the writer chooses to publish anyway` |
| AC-17 | Published articles can be edited and republished, immediately, no approval | `tests/unit/publishArticle.test.ts`, `tests/e2e/publishJourney.test.ts` | `AC-17: republishing an article that went live 3 days ago refreshes published_at, keeps first_published_at, and is flagged as a republish`; `AC-17: publishing triggers on-demand revalidation of the article, its category page and the homepage…`; `E2E-03` |
| AC-18 | Time-to-publish is recorded (start, publish, writer id) | `tests/db/schema.test.ts`, `tests/e2e/publishJourney.test.ts` | `AC-18: a draft started at 10:00 and published at 10:47 yields a 47-minute time-to-publish from the stored events alone`; `E2E-01` |

**Uncovered acceptance criteria: none.**

Product decisions from `01-decisions.md` are tagged `DEC-nn`:

| Id | Decision | Test file | Test name(s) |
|---|---|---|---|
| DEC-01 | Cover image mandatory before publish | `tests/unit/publishArticle.test.ts`, `tests/telemetry/emission.test.ts`, `tests/contract/consumer.prism.test.ts`, `tests/e2e/publishJourney.test.ts` | `DEC-01: publishing an article with no cover image at all … 400 COVER_IMAGE_REQUIRED`; `TELEMETRY-article_published (negative) / DEC-01`; `CONTRACT-CONSUMER-publishArticle / DEC-01`; `E2E-02` |
| DEC-02 | Near-duplicate taxonomy names get manual cleanup, never auto-merge | `tests/unit/taxonomy.test.ts`, `tests/db/schema.test.ts` | `DEC-02: a near-duplicate league name ("ligue1" next to "Ligue 1") is created as its own category…`; `DEC-02: "ligue1" is stored alongside "Ligue 1" instead of being merged into it…` |

ADR-specific mechanisms:

| Id | Mechanism | Test file | Test name(s) |
|---|---|---|---|
| ADR-0002 | Pronos match reference is optional/nullable, never a required FK | `tests/unit/pronosEntry.test.ts`, `tests/db/schema.test.ts` | `ADR-0002: picking a fixture from pronos stores a denormalised snapshot…`; `ADR-0002: the pronos match reference is nullable and carries no cross-project foreign key…` |
| ADR-0003 | `locked_by`/`locked_at` + heartbeat + staleness window | `tests/unit/lock.test.ts`, `tests/db/schema.test.ts`, `tests/unit/properties.test.ts` | `NFR-LOCK-01a/01b/01c`, `NFR-LOCK-02`, the two real-timing DB lock tests, `PROP-03` |

---

## 2. Measurable NFRs

Sources: `02-architecture.v1.md` §7 (STRIDE-lite, "NFR" column), §8
(observability), §4 (the egress design constraint), §6 (expand-only
migrations), and `contracts/openapi.yaml` where it fixes a number.

| NFR id | Obligation (source) | Test file | Test name(s) |
|---|---|---|---|
| NFR-AUTH-01 | All editor writes require a valid Supabase session (§7 Spoofing) | `tests/unit/publishArticle.test.ts`, `tests/unit/uploadImage.test.ts`, `tests/contract/consumer.prism.test.ts`, `tests/telemetry/emission.test.ts` | `NFR-AUTH-01: publishing a request carrying no writer bearer token is refused with 401 UNAUTHORIZED`; `NFR-AUTH-01: an image upload with no writer bearer token is rejected 401 before anything is stored`; `CONTRACT-CONSUMER-publishArticle / NFR-AUTH-01`; `TELEMETRY-article_published (negative) / NFR-AUTH-01` |
| NFR-RLS-01 | RLS enabled on every table the editor reaches via PostgREST (§7 Tampering) | `tests/db/schema.test.ts` | `NFR-RLS-01: row level security is enabled on every table the editor reaches through PostgREST` |
| NFR-RLS-02 | `anon` cannot SELECT unpublished articles (§7 Information disclosure) | `tests/db/schema.test.ts`, `tests/db/publicSiteRender.test.ts` | `NFR-RLS-02: the anon role can read a published article but never an unpublished one`; `NFR-RLS-02: no draft ever reaches the rendered public site, on the homepage or in the sitemap` |
| NFR-RLS-03 | The image bucket/table must not expose unpublished assets (§7 Information disclosure) | `tests/db/schema.test.ts` | `NFR-RLS-03: the anon role cannot read images belonging to an unpublished article` |
| NFR-TAMPER-01 | Publish-controlled columns are not directly writable by a writer (§7 Tampering) | `tests/db/schema.test.ts` | `NFR-TAMPER-01: a writer cannot set published_at directly — publish-controlled columns are not writable through PostgREST` |
| NFR-AUDIT-01 | `writer_id` retained on every publish/edit (§7 Repudiation) | `tests/db/schema.test.ts` | `NFR-AUDIT-01: every article and every telemetry row is stamped with a writer id that cannot be null` |
| NFR-UPLOAD-01 | Max upload size enforced before processing (§7 DoS; contract: 20 MB) | `tests/unit/imageOptimize.test.ts`, `tests/unit/uploadImage.test.ts`, `tests/contract/consumer.prism.test.ts` | `NFR-UPLOAD-01: a decodable JPEG over the 20 MB limit is handled distinguishably`; `NFR-UPLOAD-01: the shipped maximum upload size is the 20 MB the contract promises callers`; `NFR-UPLOAD-01: a file over the contract's 20 MB limit is rejected with 413 before the codec is ever handed the bytes`; `CONTRACT-CONSUMER-uploadArticleImage / NFR-UPLOAD-01` |
| NFR-RATE-01 | Rate limit on mutating endpoints (§7 DoS) — **threshold assumed, see §6** | `tests/unit/publishArticle.test.ts` | `NFR-RATE-01a` (10th allowed), `NFR-RATE-01b` (11th → 429), `NFR-RATE-01c` (a different IP unaffected), `NFR-RATE-01d` (the shipped constant) |
| NFR-LOCK-01 | Staleness boundary: released only once *older than* the window (ADR-0003) | `tests/unit/lock.test.ts` | `NFR-LOCK-01a` (one heartbeat old), `NFR-LOCK-01b` (exactly at threshold), `NFR-LOCK-01c` (+1 ms) |
| NFR-LOCK-02 | Heartbeat/staleness constants, window spans ≥3 heartbeats (ADR-0003) | `tests/unit/lock.test.ts` | `NFR-LOCK-02: the shipped heartbeat and staleness constants are ADR-0003's…` |
| NFR-IDEM-01 | `Idempotency-Key` on publish suppresses a duplicate telemetry emission (contract §Idempotency) | `tests/unit/publishArticle.test.ts` | `NFR-IDEM-01: replaying the same Idempotency-Key does not record a second article_published event…` |
| NFR-IDEM-02 | `Idempotency-Key` required on upload; a replay creates no second object (contract §Idempotency) | `tests/unit/uploadImage.test.ts` | `NFR-IDEM-02: replaying an upload with the same Idempotency-Key returns the original resource and uploads nothing a second time` |
| NFR-OBS-01 | A failed on-demand revalidation must be recorded, not silent (§8 Alerts) | `tests/unit/publishArticle.test.ts` | `NFR-OBS-01: a failed revalidation is recorded as a failure rather than passing silently…` |
| NFR-EGRESS-01 | Visitors never hit Supabase Storage URLs (§4: "a design constraint to enforce, not just a number to watch") | `tests/db/publicSiteRender.test.ts` | `NFR-EGRESS-01: no rendered page points a visitor at Supabase Storage, because hotlinking blows the 5 GB/month egress free tier` |
| NFR-MIGRATE-01 | Expand-only migration discipline (§6 "Migration safety: expand-only") | `tests/db/schema.test.ts` | `NFR-MIGRATE-01: no shipped migration drops or retypes an existing column or table…` |

### Property-based invariants

| Id | Invariant | Test file |
|---|---|---|
| PROP-01 | Every generated slug is URL-safe and non-empty, for any title | `tests/unit/properties.test.ts` |
| PROP-02 | A slug never collides with an existing one, and stays URL-safe while avoiding it | `tests/unit/properties.test.ts` |
| PROP-03 | Any two timestamps further apart than the staleness threshold mean the lock has expired | `tests/unit/properties.test.ts` |
| PROP-04 | No styling, class or executable node survives sanitization, for any clipboard payload | `tests/unit/properties.test.ts` |
| PROP-05 | Sanitization is idempotent | `tests/unit/properties.test.ts` |

---

## 3. API contracts — both sides

Tooling is exactly what `02-architecture.v1.md` §5 names: **Prism** for the
consumer side, **Schemathesis** for the provider side, both generated from the
contract document at run time.

### Arsène's own contract (`pdlc/arsene-cms/contracts/openapi.yaml`)

| Operation | Method + path | Consumer test (Prism) | Provider test (Schemathesis) |
|---|---|---|---|
| `publishArticle` | `POST /v1/articles/{articleId}/publish` | `CONTRACT-CONSUMER-publishArticle` (+ the `COVER_IMAGE_REQUIRED`, `IMAGE_NOT_READY`, `DRAFT_LOCKED` and `UNAUTHORIZED` branches) | `CONTRACT-PROVIDER-publishArticle` |
| `uploadArticleImage` | `POST /v1/articles/{articleId}/images` | `CONTRACT-CONSUMER-uploadArticleImage` (+ the `FILE_TOO_LARGE` and `UNSUPPORTED_FORMAT` branches) | `CONTRACT-PROVIDER-uploadArticleImage` |

`CONTRACT-COVERAGE: every operation declared in openapi.yaml has a consumer
test in this file` is a guard test that fails if an operation is ever added to
the contract without a consumer test. **It is one of the two tests that pass at
red**, because it reads only the already-delivered contract artifact.

The provider harness is wired and runs for real at red even though there is
nothing to fuzz: Schemathesis loads the contract, selects `1/2` operations per
test, runs its Examples/Coverage/Fuzzing phases and reports a network error.
That is a meaningful failure, not a silent no-op — see `03-red-evidence.v1.md`
§7 for its full output.

`CONTRACT-publish-404` additionally covers the contract's `404 NOT_FOUND`
response at the handler layer (`tests/unit/publishArticle.test.ts`).

### pronos' contract, consumed not owned (vendored)

Vendored at `pdlc/arsene-cms/contracts/vendor/pronos-openapi.yaml`, pinned to
pronos commit `d8f308de6aec0b1207df5a447edcf6fd39f77832` (see `PRONOS_SOURCE.md`);
refreshed by `scripts/update-pronos-vendor.sh`, never over the network in CI.

| Operation | Consumer test (Prism) | Provider test |
|---|---|---|
| `getCurrentGameweek` (`GET /v1/leagues/{leagueId}/current`) | `CONTRACT-CONSUMER-fixturePicker-populated`, `-emptyGameweek`, `-notFound`, `-unreachable` | **None, by construction** — pronos owns its own provider verification (`contracts/pronos-fixtures.consumer.md` §6). |

All four cases are in `tests/contract/pronos-fixtures.prism.test.ts`; the
populated case additionally schema-validates the response against
`CurrentGameweekResponse` from the vendored document, so a picker that works
against a hand-written fixture still fails.

### Datastore "contract"

There is no OpenAPI document for the editor's direct PostgREST reads/writes —
`02-architecture.v1.md` §5 says "RLS policies are the contract there, tested at
red via Testcontainers". That is `tests/db/schema.test.ts`, which applies the
**production migrations** (`db/migrations/*.sql`) to a real Postgres 16.

---

## 4. Telemetry events

Both events from the spec header's `required_events`. Shape, storage and both
emission directions are covered, because an event that only fires on success
cannot produce a completion rate — the denominator would be missing.

| Event | Shape test | Storage test | Positive emission | Negative emission |
|---|---|---|---|---|
| `draft_started` | `TELEMETRY-draft_started: builds a row carrying the writer, the article and the moment the draft started` (+ `a payload missing started_at is rejected…`) | `TELEMETRY-draft_started: the telemetry_events store accepts this required event type` | `TELEMETRY-draft_started: creating a new draft emits exactly one draft_started carrying the writer, the article and the start time` | `TELEMETRY-draft_started (negative): reopening an existing draft emits no second draft_started, so a crash-and-resume cannot reset the clock` |
| `article_published` | `TELEMETRY-article_published: builds a row carrying the writer, the article, the publish time and whether it was a republish` (+ `a payload missing is_republish is rejected…`, `a payload missing article_id is rejected…`) | `TELEMETRY-article_published: the telemetry_events store accepts this required event type` | `TELEMETRY-article_published: a successful first publish emits exactly one event, flagged as not a republish`; `TELEMETRY-article_published: republishing emits an event flagged is_republish…` | Four cases, one per way a publish can be refused: `DEC-01` (missing cover), `AC-08` (image not ready), `AC-05` (draft locked), `NFR-AUTH-01` (no bearer token) |

Registry guards, in code and in the `telemetry_events` check constraint:
`TELEMETRY-REGISTRY: the shipped registry lists exactly the two required event
types and no others`, `TELEMETRY-REGISTRY: an event type outside the agreed two
is rejected rather than silently stored` (unit), and `TELEMETRY-REGISTRY: the
store rejects an event type outside the agreed two…` (DB check constraint).

The end-to-end honesty of the pair is additionally proven against the real
Edge Function and real Postgres by `E2E-01` (both rows exist and the duration is
computable) and `E2E-02` (a refused publish writes **no** `article_published`
row).

---

## 5. End-to-end journeys

| Id | Journey | File |
|---|---|---|
| E2E-01 | draft → real 4 MB image upload → publish → live with a slug, both telemetry rows present, time-to-publish computable | `tests/e2e/publishJourney.test.ts` |
| E2E-02 | publish refused for a missing cover → article stays a draft, no `article_published` row at all | same |
| E2E-03 | republish of a 3-day-old article → live content updated immediately, no approval step, recorded as a republish | same |

---

## 6. Assumptions and flags for the product owner

1. **Lock staleness pinned at 90 s, heartbeat at 20 s.** ADR-0003 gives
   60–90 s and "roughly every 20 seconds". The suite pins the top of the range
   (`NFR-LOCK-02`). ADR-0003 itself says the number should be tuned after
   launch; changing it means changing one constant and one test.
2. **Autosave interval assumed 30 s** (`tests/unit/draft.test.ts`). AC-01 fixes
   only "over 30 seconds of typing", not the interval.
3. **Publish rate limit assumed 10 req/min/IP**, adopted from pronos'
   confirmed precedent via `02-architecture.v1.md` §7 (`NFR-RATE-01d`).
4. **Upload limit: contract's 20 MB, not the threat model's "e.g. 10MB".**
   `contracts/openapi.yaml` documents `413 FILE_TOO_LARGE` with
   `max_bytes: 20971520`; §7 says "e.g. 10MB". **These disagree.** The contract
   is a promise already made to callers, so the tests follow it — but this
   should be confirmed, not assumed, before green.
5. **The autosave indicator format** is `Last saved at HH:MM` in UTC, so the
   seam is deterministic; timezone rendering is presentation.
6. **`anon` / `authenticated` roles** are assumed to be created by the first
   migration, as pronos does, because the RLS tests `set role` to them.
7. **`draft_started` must be emitted server-side.** The editor creates drafts
   through PostgREST, not through an Edge Function, so a client-side emission
   could be lost by any client bug and the metric would silently lose its
   numerator. `E2E-01` asserts the event exists for an article inserted the way
   the SPA inserts one, which forces a trigger/RPC. **This is a design
   consequence of the metric being load-bearing; worth an explicit yes.**
8. **`NFR-TAMPER-01` assumes column-level privilege revocation** (SQLSTATE
   `42501`) rather than an RLS predicate that silently updates zero rows. Both
   satisfy the threat model; the test picks the one that fails loudly, since a
   silent zero-row update is indistinguishable from success to a client.

---

## 7. Deliberately not covered, and why

| Not covered | Reason |
|---|---|
| **The counter-metric ("writer adoption must not decline")** | `02-architecture.v1.md` §8 flags this explicitly: reverting to Word/Docs happens entirely outside Arsène, so **no event can observe it** and none was invented. The agreed proxy is a manual weekly published-article-count-per-writer review at John's gate. **This is a real, named gap, not an oversight — Pam and the product owner should confirm the proxy is acceptable.** |
| Elevation of privilege (§7) | The threat model marks it **N/A**: every writer has equal, full publish rights and no approval step exists to escalate around (spec §2, §8). There is no behaviour to assert. |
| Real Cloudflare Pages/Workers ISR revalidation | The revalidation call is asserted at its seam (`AC-17: publishing triggers on-demand revalidation…`, `NFR-OBS-01`), with the transport faked. Proving Cloudflare actually purges is a deploy concern for John's pipeline gate, not a unit under test here. |
| Rollback timing ("seconds to promote a previous deploy") | §6 says this is to be **measured and rehearsed at the pipeline gate**, not estimated. Not a test. |
| Cost / free-tier limits (§4) | Not behaviour — with one exception: the *egress design constraint* is behaviour, and is covered as `NFR-EGRESS-01`. |
| Browser/DOM rendering of the editor | Out of scope for this gate by the architecture's own testability assessment: paste sanitization is asserted at the pure-function seam that the editor must call, and the public site at the render-pass seam. No Playwright-class test exists in this suite; if one is wanted for the editor shell, it belongs to a later gate and should be argued for on its own merits. |
| Out-of-scope spec items (ads/monetisation, Wix migration, affiliate blocks, approval workflow) | Spec §8. The *absence* of an approval step is asserted positively rather than left implicit — `AC-17` and `E2E-03` both publish straight to live with no intermediate state. |
| pronos' side of the fixture contract | Arsène is purely a consumer; verifying the real pronos deployment satisfies `getCurrentGameweek` is pronos' provider-side responsibility (`contracts/pronos-fixtures.consumer.md` §6). Arsène's tests never touch a live pronos. |
| Real content quality of generated alt text / meta / advisory copy | The seams assert derivation from the article's own context (`AC-15`, `AC-13`, `AC-16`), not the eloquence of an LLM's wording, which is not a testable commitment. |
